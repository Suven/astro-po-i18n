---
import Default from "../layouts/default.astro";
import gt from "../utils/i18n";
---

<Default>
    <p
        set:html={gt.gettext(
            "This is just an example subpage. Content might follow.",
        )}
    />
</Default>
